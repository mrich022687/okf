---
type: Server
title: Q1900M — Linux Mint NAS / Backup Server (Planned: stripped headless)
description: Secondary server running Linux Mint 22.3. Provides NFS and Samba shares for backups and ISOs, plus the Android Tools library. Planned conversion to headless server.
tags: [linux, mint, nas, backup, samba, nfs, q1900, android-library]
timestamp: 2026-06-21T18:15:00Z
resource: ssh://michael@192.168.12.208
---

# Q1900M — Linux Mint NAS / Backup Server

## Overview
Secondary server running Linux Mint 22.3. Serves as a backup target and ISO repository.

- **Hostname:** q1900m (Samba: `q1900`)
- **IP:** 192.168.12.208
- **SSH:** `michael@192.168.12.208` (via `ssh q1900m`)
- **Samba:** `q1900` (was previously named `iLO`)
- **NetBIOS name:** Changed from `iLO` to `q1900` on 2026-06-21

## Storage
- **sda:** 465.8G total, 78G free — OS (ext4)
- **sdb:** 28.9G — Ubuntu 24.04.4 installer (USB)
- **sdd:** 58.7G — Retro gaming SD card (BOOT + root + EASYROMS)

## NFS Exports
| Path | Network | Purpose |
|------|---------|---------|
| `/srv/backups` | 192.168.12.0/24 | Backup storage (rw) |
| `/srv/isos` | 192.168.12.0/24 | ISO images (rw) |

## Samba Shares
| Share | Path | Purpose |
|-------|------|---------|
| `ISOs` | `/srv/isos` | ISO files |
| `Backups` | `/srv/backups` | Backup storage |

## Directory Structure
```
/srv/
├── backups/
│   ├── config-backups/     — Config backups by date
│   ├── dump/               — vzdump backup files
│   ├── usb-drive-images/   — USB drive images
│   └── okf/                — OKF knowledge base (synced from PVE)
└── isos/
    ├── Credentials/
    ├── ford-upfitter-guide/ — Ford upfitter manuals
    ├── ram/                — RAM truck guides
    ├── ram-upfitter-guides/
    ├── reference-diagrams/
    └── template/iso/
```

## Services
- **Samba** — smbd running, file sharing
- **NFS** — nfs-kernel-server running

## Backups Stored Here
- `hermes-ct106-backup.tar.zst` (999M) — CT 106 backup, copied 2026-06-21
- Various vzdump logs and images

## Android Tools Library (Added Jun 29)
The `/srv/backups/Android tools and software/` directory contains the full Android development tool repository:
- **GammaOS firmware** (6.5 GB) for RG505 flashing
- **ADB & Fastboot tools** (17 MB)
- **Unisoc VCOM drivers** for RG505
- **Emulator APKs** (empty, reserved)

See [Android Tools Library](../config/android-tools.md) for full inventory.

## Linux Mint → Headless Conversion (Pending)
**Assigned to:** Linda (or Larry if main PVE crashes)

**Current state:** Linux Mint 22.3 with XFCE desktop, Firefox with 72+ tabs (2 GB RAM), LightDM, Bluetooth, CUPS, ModemManager, accounts-daemon, colord — all unnecessary for a file server.

**Plan:**
1. Kill Firefox (frees ~2 GB RAM immediately)
2. Remove XFCE desktop: `apt purge xfce4* lightdm`
3. Disable junk services: `systemctl disable --now bluetooth cups ModemManager accounts-daemon avahi-daemon colord`
4. Set up SSH key auth (passwordless login)
5. Save credentials to vault (done: `michael` / `mcrart8794`)
6. Uninstall Firefox: `apt purge firefox*`

**Credentials:** Stored in vault under `Q1900-NAS-iLO` (michael / mcrart8794)
